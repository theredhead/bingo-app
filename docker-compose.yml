services:
  bingo:
    image: theredhead/bingo-game:latest
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATABASE_PATH=${DATABASE_PATH:-/app/db.sqlite}
      - FRONTEND_URL=${FRONTEND_URL:-}
    restart: unless-stopped
